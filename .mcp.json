{
  "mcpServers": {
    "ics-calendar": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/index.js"],
      "env": {
        "ICS_BRIDGE_BIN": "${CLAUDE_PLUGIN_ROOT}/bin/ICSBridge.app"
      }
    }
  }
}
